# ============================================================
# GINI-ORACLE-1 — Python Dependencies
# Offline-first. No cloud API SDKs required.
# Install with: pip install -r requirements.txt
# ============================================================

# --- Core Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic>=2.9.0
pydantic-settings>=2.4.0

# --- Local Intelligence (offline, no API keys) ---
vaderSentiment==3.3.2
textblob==0.18.0

# --- Voice ---
speechrecognition==3.10.4
pyttsx3==2.90
# pyaudio==0.2.14  # Install separately if needed (requires PortAudio)

# --- Device Control / IoT ---
paho-mqtt==2.0.0
requests==2.32.2
websockets==12.0

# --- Utilities ---
python-dotenv==1.0.1
loguru==0.7.2
rich==13.7.1
httpx==0.27.0
aiofiles==23.2.1
apscheduler==3.10.4

# --- Database ---
sqlalchemy>=2.1.0b2
alembic==1.13.1

# --- Testing ---
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# --- Dev Tools ---
black==24.4.2
flake8==7.0.0
mypy==1.10.0

# --- Optional: Local LLM (no API keys needed) ---
# Uncomment to use Ollama programmatically (optional):
# ollama>=0.1.0
